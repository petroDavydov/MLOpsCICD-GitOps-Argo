## namespaces/application/nginx.yaml

# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: mlflow
#   namespace: infra-tools
# spec:
#   project: default
#   source:
#     repoURL: https://community-charts.github.io/helm-charts
#     chart: mlflow
#     targetRevision: 0.6.2
#     helm:
#       valueFiles:
#         - values/mlflow-values.yaml
#   destination:
#     server: https://kubernetes.default.svc
#     namespace: mlflow
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
#     syncOptions:
#       - CreateNamespace=true



# nginx.yaml не використовується напряму, бо nginx деплоїться через Helm-чарт.
# Деплой відбувається через ArgoCD Application з values/nginx-values.yaml
